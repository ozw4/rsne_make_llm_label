# RSNA report labeler: development instructions

この作業ツリーは、膝MRIレポートをCodexに1件ずつ読ませる実行環境専用である。
このファイルは実行スクリプト・検証・devcontainerを実装、保守するCodex向けであり、レポート判定の指示ではない。

## 作業範囲

現在の作業ツリーにあるファイルだけを使う。元の学習リポジトリ、他ブランチ、履歴、アーカイブを探索しない。
画像学習、推論、Kaggle提出、既存の学習ラベルの変更は対象外とする。
レポート本文、生成ラベル、実行ログ、認証情報はGit管理外に置く。認証情報を表示しない。

## 読解の契約

判定指示の正本は `prompts/codex_report_labeling_instructions_v1.md` とする。
公式定義、4状態、12ターゲット、原文根拠の出力契約を、明示的な依頼なしに変更しない。
判定指示は読解専用作業ディレクトリの `AGENTS.md` へそのままコピーして自動読込させる。標準入力へ重複して追加しない。
読解用Codexは、この開発用AGENTS.mdを読み込まない作業ディレクトリと専用CODEX_HOMEから起動する。

各レポートおよび各再試行につき、新しい `codex exec` プロセスを起動する。
`resume`、`fork`、サブエージェント、前件の履歴・メモリの継承、1セッションへの複数レポート投入を実装しない。
可変入力は `study_id` と `report` のJSON1件だけとする。既存ラベル、gold、画像予測は渡さない。
モデルにはレポートの読解とJSON回答だけを担当させる。ファイル探索、保存、外部検索を任せない。

## スクリプトの責務

入力の取り出し、Codex起動、形式検証、保存、使用量記録、未完了分の再実行に限定する。
検証は入力IDとの一致、12キー、許可された状態、原文根拠の部分文字列一致などの出力契約を確認する。
医学的な正規表現判定器、病名辞書、既存ラベルによる補正処理を作らない。
失敗をnegativeやnot_mentionedへ置換しない。検証済み結果だけを完了として保存する。
再開時は本文・判定指示・実行設定が一致する検証済み結果だけをスキップする。異なる版は別出力先に分ける。

## 実装方針

関数は小さく単一責務にする。初版は逐次処理とし、必要のない抽象化や分散処理を追加しない。
テストには人工レポートとCodex呼び出しのモックを使い、通常のテストで有料の実行を発生させない。
